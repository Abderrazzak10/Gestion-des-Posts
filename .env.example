# Example environment variables for Gestion des Posts

# Server configuration
PORT=8000

# Database configuration
MONGO_URI=mongodb://localhost:27017/gestion-des-posts
#ou
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority&appName=<AppName>
# Replace <username> with your MongoDB username
# Replace <password> with your MongoDB password
# Replace <database> with your database name
# Replace <AppName> with your application name
#Replace <cluster> with your MongoDB cluster name


# A random string used for signing tokens
TOKEN_SECRET=secret


# Other configurations
# NODE_ENV specifies the environment in which the application is running.
# Valid values are:
# - development: for local development
# - production: for the live production environment
# - test: for running automated tests
NODE_ENV=development